TICKETMASTER_API_KEY=your_ticketmaster_api_key_here

# Number of months ahead from today to pull events for (inclusive of today).
# Used by the ingestion Lambda to compute the end date filter.
EVENTS_LOOKAHEAD_MONTHS=1
